# Teacher Admin sidebar menu verification

- Root dashboard: the sidebar three-dot control opens a menu with `Update Profile`, `Settings`, `Contact Support`, and `Log Out`.
- Root dashboard: selecting `Settings` navigates to `/settings` and closes the menu.
- Routed page: the sidebar three-dot control on `/settings` opens the same four options.
- Upload New Exam Paper: the root dashboard upload card now opens `Import Students` with the title `Upload New Exam Paper`; clicking the X closes it without the previous undefined `type` crash.
